type Protocol @entity {
  id: ID!
  totalVolume: BigDecimal!
}

type Pool @entity {
  id: ID!
  totalVolume: BigDecimal!
  protocol: String!
  asset0: Bytes!
  asset1: Bytes!
}

type Asset @entity {
  id: ID!
  totalVolume: BigDecimal!
  decimals: Int!
}
