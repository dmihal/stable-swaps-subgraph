type Protocol @entity {
  id: ID!
  totalVolume: BigDecimal!
}

type Pool @entity {
  id: ID!
  totalVolume: BigDecimal!
  protocol: String!
  assets: [Bytes!]!
}

type Asset @entity {
  id: ID!
  totalVolume: BigDecimal!
  decimals: Int!
}
