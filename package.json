{
  "name": "stable-swaps",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ dmihal/stable-swaps",
    "create-local": "graph create --node http://localhost:8020/ dmihal/stable-swaps",
    "remove-local": "graph remove --node http://localhost:8020/ dmihal/stable-swaps",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dmihal/stable-swaps"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.1",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
